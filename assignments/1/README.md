# 作业 1

说明：此目录为作业 `1` 的工程目录，包含参考实现与测试平台。

目录结构：

- `src/verilog/` : 设计源码
- `tb/` : 测试平台

运行方式：在根目录运行根目录下的仿真脚本并指定文件，例如：

```powershell
cd 'd:\Program\数电实践'
.\scripts\run-sim.bat alu16 assignments\1\src\verilog\alu16.sv assignments\1\tb\alu16_tb.sv
```

提交说明：把 `assignments/1/src` 下的源码和 `assignments/1/tb` 下的测试平台一并打包提交。
